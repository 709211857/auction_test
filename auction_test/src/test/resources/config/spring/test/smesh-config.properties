smesh.localServiceProvider.name = ${spring.application.name}
smesh.localServiceProvider.serverCapability = full

smesh.cluster.enabled = true
smesh.call.defaultTimeoutMillis = 60000
smesh.hazelcast.serializer = fst
smesh.hazelcast.network.tcpIpMembers.resolveByName = true

smesh.serviceScanner.basePackages = com.infonova,com.optus.owunity
spring.application.name = si-appointment-test